
  # Anubis

  This is a code bundle for Anubis. The original project is available at https://www.figma.com/design/rfNn7UdNl2QCgNSkuUByXb/Anubis.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  